---
description: バージョンアップ時の必須更新ファイルとリリース手順
globs:
alwaysApply: true
---

# バージョンアップ

CHANGELOG.md の `## [Unreleased]` に変更内容を書き、main のクリーンな作業ツリーで次を実行する：

```bash
npm run release -- <patch|minor|major>
```

セマンティックバージョニングに従い、破壊的変更は major、後方互換の機能追加は minor、バグ修正と小さな改善は
patch を指定する。

## バージョンアップの手順

1. コードの変更を完了し、CHANGELOG.md の `## [Unreleased]` に分類した変更内容を書く
2. `npm run release -- <patch|minor|major>` を実行する
   - release が **`npm run check` を実行**する（lint → 整形チェック → 単体テスト → smoke）
   - smoke（`test/smoke/run.js`）は `test/dai-html` で本番ビルドと開発サーバーを実際に動かし、
     設定の反映・SCSS/JS/画像の監視追従を検証する。約 1 分
   - 落ちたらリリースしない。手書きのテストレポートは廃止（結果は script の出力と CI が持つ）
3. release が package.json / package-lock.json / CHANGELOG.md / README.md の3系統のリリースファイルを自動更新する
4. release が commit、annotated tag、main とタグの push、利用可能なら GitHub Release 作成まで自動で行う

ファイルや Git を変更せず実行予定を確認する場合は `--dry-run` を付ける。`npm run check` を意図的に飛ばす場合は
`--skip-check` を付け、表示される WARN を確認する。

## その他のルール

- コミットメッセージは日本語で記述
- 設定ファイルの変更は慎重に（後方互換性を考慮）
- 新機能追加時は example ファイルも更新
- 単体テスト（`test/unit/`）で回帰を固定できる修正は、修正前に失敗するテストを先に書く
